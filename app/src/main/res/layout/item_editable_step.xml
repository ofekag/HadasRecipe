<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/step_phase"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        tools:text="1)"
        android:textColor="@color/black"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/step_description"
        android:layout_width="0dp"
        tools:text="Break 2 Eggs Into A Bowl"
        android:layout_height="wrap_content"
        android:inputType="textAutoComplete"
        android:ellipsize="end"
        android:hint="Step Description"
        android:maxLines="1"
        app:layout_constraintEnd_toStartOf="@+id/remove_step"
        app:layout_constraintStart_toEndOf="@+id/step_phase"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/remove_step"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/ic_baseline_remove_24"
        android:layout_width="wrap_content"
        android:layout_marginEnd="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_height="wrap_content"/>


</androidx.constraintlayout.widget.ConstraintLayout>